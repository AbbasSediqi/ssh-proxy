# پنل مدیریت تونل SSH (مخصوص سرور ایران) 🛡️

این اسکریپت برای ایجاد تونل‌های SOCKS5 **کاملاً امن و داخلی** طراحی شده است.

## 🔒 امنیت (Internal Use Only)
برخلاف اکثر اسکریپت‌ها، این پنل پورت‌ها را فقط روی `127.0.0.1` باز می‌کند. این یعنی:
- **فقط برنامه‌های داخل خودِ سرور** (مثل پنل های x-ui،ربات‌های تلگرام، اسکریپت‌های پایتون و...) می‌توانند از تونل استفاده کنند.
- **هیچ‌کس از بیرون سرور** نمی‌تواند به این پورت‌ها وصل شود یا از پهنای باند شما سوءاستفاده کند.

## 📥 نصب سریع
برای نصب، دستور زیر را در ترمینال کپی کنید:

```bash
wget -qO- [https://raw.githubusercontent.com/AbbasSediqi/ssh-proxy/main/ssh-proxy.sh](https://raw.githubusercontent.com/AbbasSediqi/ssh-proxy/main/ssh-proxy.sh) | tr -d '\r' > setup.sh && bash setup.sh
