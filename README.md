# پنل مدیریت تونل SSH (ssh-proxy) 🚀

یک اسکریپت ساده، هوشمند و قدرتمند برای مدیریت تونل‌های SSH در سرورهای ایران. با استفاده از این پنل می‌توانید به راحتی چندین تونل SOCKS5 بسازید، وضعیت آن‌ها را مانیتور کنید و کیفیت اتصال (پینگ و سرعت) را بسنجید.



## ✨ ویژگی‌های برجسته
* **نصب و راه‌اندازی آسان:** فقط با یک خط کد.
* **مدیریت هوشمند:** امکان ساخت، متوقف کردن و حذف چندین تونل به صورت همزمان.
* **تست کیفیت:** سنجش پینگ (Latency) و تست سرعت دانلود واقعی از داخل تونل.
* **رابط کاربری عددی:** انتخاب تونل‌ها بر اساس شماره (Index) بدون نیاز به تایپ پورت.
* **پایداری بالا:** اجرای تونل‌ها به عنوان سرویس سیستم‌عاملی (Systemd) و اجرای خودکار هنگام ریبوت سرور.
* **حذف کامل:** دارای گزینه آن‌اینستالر برای پاکسازی تمام ردپای اسکریپت و سرویس‌ها.

---

## 📥 نصب آسان (One-Line Install)
برای نصب پنل و راه‌اندازی اولین تونل، کافیست دستور زیر را کپی کرده و در ترمینال سرور ایران خود اجرا کنید:

```bash
bash <(curl -sSL [https://raw.githubusercontent.com/AbbasSediqi/ssh-proxy/main/ssh-proxy.sh](https://raw.githubusercontent.com/AbbasSediqi/ssh-proxy/main/ssh-proxy.sh))
